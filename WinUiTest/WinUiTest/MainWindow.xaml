<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUiTest.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUiTest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <StackPanel Orientation="Vertical" Width="288">
        <GridView>
            
        </GridView>
        <StackPanel x:Name="RootPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Height="160">
            <ProgressRing Loaded="ProgressRing_Loaded" IsActive="{Binding IsBusy}"/>
            <TextBlock Text="名前"></TextBlock>
            <TextBox x:Name="AnimalName" Text="{Binding Name}" IsReadOnly="{Binding IsBusy}"></TextBox>
            <Button x:Name="myButton" Click="myButton_Click">Click Me</Button>

        </StackPanel>
        <ProgressBar IsIndeterminate="True" ShowError="False" ShowPaused="False"></ProgressBar>
    </StackPanel>
</Window>
